spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ITLens
    username: postgres
    password: @aahmhmm28
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  profiles:
    active: dev
server:
  port: 8080